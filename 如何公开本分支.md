# 如何公开本分支（且不泄漏代码）

GitHub / GitLab **无法**在同一个仓库里做到「只公开某一个分支、其它分支保密」。  
仓库一旦设为 Public，**所有分支与历史**都会可见。

## 推荐做法（安全）

把本 `download` 分支推到一个**单独的公开仓库**，开发代码留在**私有仓库**：

```bash
# 1. 在 GitHub 新建空仓库，例如：EssSimulator-Download（Public）
# 2. 本机在 download 分支上：
git remote add public git@github.com:<你的账号>/EssSimulator-Download.git
git push -u public download:main
```

之后对外只分享公开仓库链接；私有仓库继续开发，不要把私有 remote 设成 Public。

## 不推荐

- 把现有私有仓直接改成 Public（即使只想给人看 download，历史里的源码也会暴露）
- 从带完整历史的分支 `git checkout -b download` 再删文件（历史里仍有代码，别人可以 `git checkout` 旧提交）

本分支使用 **orphan（无父提交）** 创建，历史上不挂载任何源码提交。

## 更新介绍页

1. 切到私有仓的 `download` 分支（或在公开仓直接改）
2. 替换 `assets/screenshots/` 下的真实运行截图
3. 更新 [商业化.md](./商业化.md) 中的售价、联系方式
4. `git push public download:main`
